// GROVIA Admin Runtime Fix — intentionally disabled.
// Users & Workspaces is owned by users-console.js.
// This legacy runtime must not overwrite the QA-approved Admin UI.
(function(){
  window.groviaAdminRuntimeDisabled = true;
})();
