import {Router} from 'express';import {requireUser} from '../middleware/auth.js';import {apiOk,apiError} from '../utils/response.js';const r=Router();r.get('/session',requireUser,(req,res)=>res.json(apiOk({user:req.user})));r.get('/admin-check',requireUser,async(req,res)=>{if(!req.serviceClient)return res.status(503).json(apiError('ADMIN_NOT_CONFIGURED','Admin backend belum dikonfigurasi.'));const {data,error}=await req.serviceClient.from('grovia_admin_roles').select('role,active').eq('user_id',req.user.id).eq('active',true).maybeSingle();if(error)return res.status(500).json(apiError('ADMIN_CHECK_FAILED','Gagal memeriksa role admin.'));res.json(apiOk({isAdmin:!!data,role:data?.role||null}));});export default r;
